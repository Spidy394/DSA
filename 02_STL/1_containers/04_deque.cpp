# include <bits/stdc++.h>
using namespace std;

int main() {
    deque<int> dq;

    dq.push_back(1); // {1}
    dq.emplace_back(2); // {1, 2}

    /*
        ..
        ...

    */

    // rest functions are same as vectors 
    // begin, end, rbegin, rend, clear, insert, size, swap

    return 0;
}