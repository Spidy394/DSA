# include <bits/stdc++.h>
using namespace std;

int main() {
    unordered_set<int> ust;
    // same as set but does not have a fixed order 
    // TC => O(1)
    // wc => O(n) , very rare

    // all other operations other then lower and upper bound works

    return 0;
}